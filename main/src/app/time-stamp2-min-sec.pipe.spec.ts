import { TimeStamp2MinSecPipe } from './time-stamp2-min-sec.pipe';

describe('TimeStamp2MinSecPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeStamp2MinSecPipe();
    expect(pipe).toBeTruthy();
  });
});
